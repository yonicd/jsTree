---
title: "jsTree"
author: "Jonathan Sidi"
date: "May 9, 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R markdown document showing functionality of jsTree

```{r}
library(jsTree)
data(states)
data(state_bird)
```

data can be a data.frame or a string that is collapse columns to text (with seperator)

```{r}
x <- jsTree::jsTree$new(states)

x$show()
```

Add tooltips to state names with the state bird
```{r}

x$tooltips <- state_bird

x$show()
```

initialize tree with checked boxes for certain fields
```{r}
x$nodestate(variable=='Area')
x$show()
```

```{r}
x$nodestate(variable=='Area'&grepl('^M',state.name))
x$show()
```

```{r}

x$nodestate((variable %in% c('Murder') & value >= 10) | (variable %in% c('HS.Grad') & value <= 55) )
x$show()

 #remove node states
x$nodestate(NULL)

```

change the order of the hierarchy
```{r}
#using character object as data
x$data <- apply(states[,c(4,1,2,3,5)],1,paste,collapse='/')
x$show()
```

Use other delimiters to define the heirarchy
```{r}
x$data <- states
x$sep <- '|-|'
x$show()
```